name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Lint Markdown (markdownlint)
        run: npx --yes markdownlint-cli "docs/**/*.md" "skills/**/*.md"
